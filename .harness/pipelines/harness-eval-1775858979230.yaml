pipeline:
  name: Build harness-eval
  identifier: Build_harness_eval_1775858987359
  projectIdentifier: default_project
  orgIdentifier: default
  stages:
    - stage:
        name: Build
        identifier: Build
        type: CI
        spec:
          cloneCodebase: true
          execution:
            steps:
              - step:
                  type: GitClone
                  name: CloneRepo
                  identifier: CloneRepo
                  spec:
                    connectorRef: account.jpbartogithub
                    repoName: harness-eval
                    build:
                      type: branch
                      spec:
                        branch: main
              - step:
                  type: Run
                  name: Build Helm Chart
                  identifier: Build_Helm_Chart
                  spec:
                    shell: Sh
                    command: helm package helm/guestbook --version '1.0.7'
          platform:
            os: Linux
            arch: Amd64
          runtime:
            type: Cloud
            spec: {}
    - stage:
        name: Deliver
        identifier: Deliver
        description: ""
        type: Custom
        spec:
          execution:
            steps:
              - step:
                  type: ShellScript
                  name: Push to TTL
                  identifier: Push_to_TTL
                  spec:
                    shell: Bash
                    executionTarget: {}
                    source:
                      type: Inline
                      spec:
                        script: helm push helm-guestbook-1.0.7.tgz 'oci://ttl.sh/jpbarto/charts/helm-guestbook'
                    environmentVariables: []
                    outputVariables: []
                  timeout: 10m
        tags: {}
  properties:
    ci:
      codebase:
        connectorRef: account.jpbartogithub
        repoName: harness-eval
        build: <+input>
